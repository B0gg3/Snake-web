
var highScores = [] // dictionary to keep all the highscores temps

// place holders
highScores.push(["None", 0])
highScores.push(["None", 0])
highScores.push(["None", 0])
highScores.push(["None", 0])
highScores.push(["None", 0])

// get the empty highscores
export function getHighScores() {
    return highScores
}
